name: Auto Hotfix Sync
run-name: Auto Hotfix Sync - Run #${{ github.run_number }}

on:
  push:
    branches:
      - rel/env-prod
  workflow_dispatch: # Manuel tetikleme için

jobs:
  sync-hotfix:
    runs-on: ubuntu-latest
<<<<<<< HEAD
<<<<<<< HEAD
    
=======
    timeout-minutes: 15 # Timeout ekle

>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)
=======

>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
<<<<<<< HEAD
          token: ${{ secrets.GITHUB_TOKEN }}
      
=======
          token: ${{ secrets.PAT_TOKEN }}

>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
      - name: Configure Git
        run: |
          git config user.name "ubeniz"
          git config user.email "umitbeniz@araskargo.com.tr"

      - name: Fetch branches
        run: |
          git fetch origin rel/env-test
          git fetch origin rel/env-prod

      - name: Find and analyze commits
        id: analyze
        shell: bash
        run: |
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
          MISSING=$(git log origin/rel/env-test..origin/rel/env-prod --format="%H|%s")
<<<<<<< HEAD
=======
          set -e
>>>>>>> c56d26b (Optimize hotfix detection with patch-id caching)
          
=======
          set -e
          
<<<<<<< HEAD
          # Prod'da olup test'te olmayan commitler
<<<<<<< HEAD
          MISSING=$(git log origin/rel/env-test..origin/rel/env-prod --format="%H")
=======
          MISSING=$(git log origin/rel/env-test..origin/rel/env-prod --format="%H|%s")
>>>>>>> bb07eeb (Improve commit synchronization and conflict handling)
=======
          MISSING=$(git log origin/rel/env-test..origin/rel/env-prod --format="%H|%s")
>>>>>>> 47a3469 (Refactor commit synchronization logic in workflow)
=======
          set -e  # Hata durumunda dur
          
          MISSING=$(git log origin/rel/env-prod..origin/rel/env-test --format="%H|%s")
>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)

>>>>>>> 976f848 (Refactor commit synchronization logic in auto-hotfix.yml)
=======

>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
          if [ -z "$MISSING" ]; then
            echo "✅ Branches are synchronized"
            echo "needs_sync=false" >> $GITHUB_OUTPUT
            exit 0
          fi
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
=======

<<<<<<< HEAD
          # Test branch'teki tüm patch-id'leri dosyaya yaz (bir kere)
          echo "📊 Building patch-id cache for rel/env-test..."
          CACHE_FILE=$(mktemp)
          git log origin/rel/env-test --format="%H" | while read -r test_commit; do
<<<<<<< HEAD
            git show "$test_commit" | git patch-id --stable | cut -d' ' -f1
          done > "$CACHE_FILE"
>>>>>>> 47a3469 (Refactor commit synchronization logic in workflow)
          
=======

>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
          COMMITS_TO_SYNC=""
<<<<<<< HEAD
<<<<<<< HEAD
          while IFS='|' read -r hash message; do
            PROD_PATCH_ID=$(git show $hash | git patch-id | cut -d' ' -f1)
            FOUND=false

            for test_commit in $(git log origin/rel/env-test --format="%H"); do
              TEST_PATCH_ID=$(git show $test_commit | git patch-id | cut -d' ' -f1)
              if [ "$TEST_PATCH_ID" = "$PROD_PATCH_ID" ]; then
                FOUND=true
                break
              fi
            done

            if [ "$FOUND" = false ]; then
=======
          # Önce tüm test branch commit'lerinin patch-id'lerini bir kere al
          echo "📊 Building patch-id cache for rel/env-test..."
          declare -A TEST_PATCHES
          while read -r test_commit; do
            TEST_PATCH_ID=$(git show $test_commit | git patch-id --stable | cut -d' ' -f1)
            TEST_PATCHES[$TEST_PATCH_ID]=1
          done < <(git log origin/rel/env-test --format="%H")

          COMMITS_TO_SYNC=""
          COMMIT_COUNT=0
          
          while IFS='|' read -r hash message; do
            PROD_PATCH_ID=$(git show $hash | git patch-id --stable | cut -d' ' -f1)
            
            # Hash lookup ile O(1) kontrol
            if [ -z "${TEST_PATCHES[$PROD_PATCH_ID]}" ]; then
>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)
              COMMITS_TO_SYNC="$COMMITS_TO_SYNC$hash,"
              ((COMMIT_COUNT++))
              echo "🔥 Hotfix detected: ${hash:0:7} - $message"
            else
              echo "✓ Already in test: ${hash:0:7} - $message"
            fi
          done <<< "$MISSING"

          COMMITS_TO_SYNC=${COMMITS_TO_SYNC%,}

          if [ -z "$COMMITS_TO_SYNC" ]; then
=======
          COMMIT_COUNT=0
          ALREADY_APPLIED=0
          
          # git cherry çıktısını önce kontrol et
          CHERRY_OUTPUT=$(git cherry origin/rel/env-test origin/rel/env-prod 2>&1 || true)
          
          if [ -z "$CHERRY_OUTPUT" ]; then
            echo "⚠️ git cherry returned empty output, falling back to simple comparison"
            # Fallback: tüm commitleri sync olarak işaretle
            while IFS='|' read -r hash message; do
              [ -z "$hash" ] && continue
              echo "🔥 Needs sync: ${hash:0:7} - $message"
              [ -n "$COMMITS_TO_SYNC" ] && COMMITS_TO_SYNC="${COMMITS_TO_SYNC},"
              COMMITS_TO_SYNC="${COMMITS_TO_SYNC}${hash}"
              ((COMMIT_COUNT++))
            done <<< "$MISSING"
          else
            echo "git cherry output:"
            echo "$CHERRY_OUTPUT" | head -10
            echo ""
            
            # git cherry çıktısını parse et
            while read -r line; do
              [ -z "$line" ] && continue
              
              status=$(echo "$line" | cut -c1)
              commit_hash=$(echo "$line" | awk '{print $2}')
              
              if [ "$status" = "+" ]; then
                COMMIT_MSG=$(git log -1 --format="%s" "$commit_hash" 2>/dev/null || echo "Unknown")
                echo "🔥 Needs sync: ${commit_hash:0:7} - $COMMIT_MSG"
                
                [ -n "$COMMITS_TO_SYNC" ] && COMMITS_TO_SYNC="${COMMITS_TO_SYNC},"
                COMMITS_TO_SYNC="${COMMITS_TO_SYNC}${commit_hash}"
                ((COMMIT_COUNT++))
              elif [ "$status" = "-" ]; then
                COMMIT_MSG=$(git log -1 --format="%s" "$commit_hash" 2>/dev/null || echo "Unknown")
                echo "✓ Already applied: ${commit_hash:0:7} - $COMMIT_MSG"
                ((ALREADY_APPLIED++))
              fi
            done <<< "$CHERRY_OUTPUT"
          fi
<<<<<<< HEAD
=======
            git show $test_commit | git patch-id --stable | cut -d' ' -f1
          done > "$CACHE_FILE"
=======
      
      - name: Cherry-pick commits to new branch
        if: steps.analyze.outputs.needs_sync == 'true'
        id: cherry_pick
        run: |
          echo "conflict_detected=false" >> $GITHUB_ENV
          git checkout -b auto-hotfix-sync-${{ github.run_number }} origin/rel/env-test
>>>>>>> 9ff80ce (Fix condition for adding comment on conflict detection)
          
          echo "✅ Cache built: $(wc -l < "$CACHE_FILE") patches"
>>>>>>> c56d26b (Optimize hotfix detection with patch-id caching)

          echo ""
          echo "📊 Analysis complete:"
          echo "  - Already applied: $ALREADY_APPLIED"
          echo "  - Need to sync: $COMMIT_COUNT"

          if [ $COMMIT_COUNT -eq 0 ]; then
            echo ""
            echo "✅ All changes already in test branch"
>>>>>>> 6fd1efa (Refactor commit sync analysis in auto-hotfix.yml)
=======
          COMMIT_COUNT=0
          
          echo "$MISSING" | while IFS='|' read -r hash message; do
            [ -z "$hash" ] && continue
            
            PROD_PATCH_ID=$(git show "$hash" | git patch-id --stable | cut -d' ' -f1)
            
            # grep ile kontrol
            if ! grep -q "^${PROD_PATCH_ID}$" "$CACHE_FILE"; then
              COMMITS_TO_SYNC="${COMMITS_TO_SYNC}${hash},"
              COMMIT_COUNT=$((COMMIT_COUNT + 1))
              echo "🔥 Hotfix detected: ${hash} - $message" | cut -c1-100
            else
              echo "✓ Already in test: ${hash} - $message" | cut -c1-100
            fi
          done
          
          # Subshell problemini çöz - while loop içindeki değişkenler dışarı çıkmıyor
          # Tekrar hesapla
          COMMITS_TO_SYNC=""
          COMMIT_COUNT=0
          
          while IFS='|' read -r hash message; do
            [ -z "$hash" ] && continue
            
<<<<<<< HEAD
            PROD_PATCH_ID=$(git show "$hash" | git patch-id --stable | cut -d' ' -f1)
            
            if ! grep -q "^${PROD_PATCH_ID}$" "$CACHE_FILE"; then
              if [ -z "$COMMITS_TO_SYNC" ]; then
                COMMITS_TO_SYNC="$hash"
              else
                COMMITS_TO_SYNC="${COMMITS_TO_SYNC},${hash}"
              fi
              COMMIT_COUNT=$((COMMIT_COUNT + 1))
=======
            # grep ile O(n) ama toplam O(n*m) - yine de orijinalden iyi
            if ! grep -q "^${PROD_PATCH_ID}$" "$CACHE_FILE"; then
              COMMITS_TO_SYNC="$COMMITS_TO_SYNC$hash,"
              ((COMMIT_COUNT++))
              echo "🔥 Hotfix detected: ${hash:0:7} - $message"
            else
              echo "✓ Already in test: ${hash:0:7} - $message"
>>>>>>> c56d26b (Optimize hotfix detection with patch-id caching)
            fi
          done <<EOF
          $MISSING
          EOF

          rm -f "$CACHE_FILE"
<<<<<<< HEAD

          if [ "$COMMIT_COUNT" -eq 0 ]; then
            echo "✅ All commits already in test branch"
>>>>>>> 47a3469 (Refactor commit synchronization logic in workflow)
=======
          COMMITS_TO_SYNC=${COMMITS_TO_SYNC%,}

          if [ -z "$COMMITS_TO_SYNC" ]; then
<<<<<<< HEAD
            echo "✅ All commits already in test branch"
>>>>>>> c56d26b (Optimize hotfix detection with patch-id caching)
=======
            echo "✅ All commits already in test branch (different hashes, same changes)"
>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)
            echo "needs_sync=false" >> $GITHUB_OUTPUT
          else
            echo "🔥 Found $COMMIT_COUNT commit(s) to sync"
            echo "needs_sync=true" >> $GITHUB_OUTPUT
            echo "commits=$COMMITS_TO_SYNC" >> $GITHUB_OUTPUT
            echo "commit_count=$COMMIT_COUNT" >> $GITHUB_OUTPUT
          fi
<<<<<<< HEAD
      
      - name: Cherry-pick to new branch
=======

          MISSING_COUNT=$(echo "$MISSING" | wc -l)
          echo "📊 Commits in prod not in test (by hash): $MISSING_COUNT"
          echo ""
          
          # Basit ama etkili: Sadece commit hash'lerini kullan
          # GitHub UI "able to merge" diyorsa, cherry-pick başarılı olur
          COMMITS_TO_SYNC=$(echo "$MISSING" | tr '\n' ',' | sed 's/,$//')
=======

          echo "📊 Commits in prod not in test (by hash): $(echo "$MISSING" | wc -l)"
          
          # Akıllı filtreleme: git cherry ile gerçekten uygulanması gereken commitleri bul
          echo ""
          echo "🔍 Analyzing which commits actually need to be applied..."
          
          COMMITS_TO_SYNC=""
          COMMIT_COUNT=0
          
          # git cherry: test branch'e göre hangi commitler eksik?
          # + ile başlayanlar = uygulanmamış
          # - ile başlayanlar = zaten uygulanmış (farklı hash ama aynı değişiklik)
          while read -r status commit_hash rest; do
            if [ "$status" = "+" ]; then
              # Bu commit gerçekten uygulanması gerekiyor
              COMMIT_MSG=$(git log -1 --format="%s" "$commit_hash")
              echo "🔥 Needs sync: ${commit_hash:0:7} - $COMMIT_MSG"
              
              [ -n "$COMMITS_TO_SYNC" ] && COMMITS_TO_SYNC="${COMMITS_TO_SYNC},"
              COMMITS_TO_SYNC="${COMMITS_TO_SYNC}${commit_hash}"
              ((COMMIT_COUNT++))
            else
              # Bu commit'in değişikliği zaten test'te var
              COMMIT_MSG=$(git log -1 --format="%s" "$commit_hash")
              echo "✓ Already applied: ${commit_hash:0:7} - $COMMIT_MSG"
            fi
          done < <(git cherry origin/rel/env-test origin/rel/env-prod)

          if [ $COMMIT_COUNT -eq 0 ]; then
            echo ""
            echo "✅ All changes already in test branch (via cherry-pick or merge)"
            echo "needs_sync=false" >> $GITHUB_OUTPUT
          else
            echo ""
            echo "🔥 Found $COMMIT_COUNT commit(s) that need to be synced"
            echo "needs_sync=true" >> $GITHUB_OUTPUT
            echo "commits=$COMMITS_TO_SYNC" >> $GITHUB_OUTPUT
            echo "commit_count=$COMMIT_COUNT" >> $GITHUB_OUTPUT
          fi
          
=======

>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
      - name: Cherry-pick commits to new branch
        if: steps.analyze.outputs.needs_sync == 'true'
        id: cherry_pick
        shell: bash
        run: |
<<<<<<< HEAD
<<<<<<< HEAD
          set +e
          
          BRANCH_NAME="auto-hotfix-sync-${{ github.run_number }}"
          git checkout -b "$BRANCH_NAME" origin/rel/env-test
=======
          set +e  # Cherry-pick hataları için devam et
          
          BRANCH_NAME="auto-hotfix-sync-${{ github.run_number }}"
          git checkout -b $BRANCH_NAME origin/rel/env-test
>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)
          
          CONFLICT_DETECTED=false
          CONFLICT_COMMITS=""
          SUCCESS_COMMITS=""
<<<<<<< HEAD
          SKIPPED_COMMITS=""
=======
>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)

          IFS=',' read -ra COMMITS <<< "${{ steps.analyze.outputs.commits }}"

          for commit in "${COMMITS[@]}"; do
<<<<<<< HEAD
            echo ""
<<<<<<< HEAD
            echo "=========================================="
            echo "🔄 Processing commit: $commit"
            git log --oneline -1 "$commit"
            echo "=========================================="
            
            # Cherry-pick yap
            if git cherry-pick "$commit" 2>&1; then
              echo "✅ Cherry-pick successful"
=======
            echo "🔄 Cherry-picking $commit..."
            
            # Cherry-pick yap
            git cherry-pick "$commit"
            CHERRY_PICK_EXIT=$?
            
            if [ $CHERRY_PICK_EXIT -eq 0 ]; then
              # Başarılı
              echo "✅ Success: $commit"
>>>>>>> ee092da (Improve cherry-pick process in auto-hotfix workflow)
              [ -n "$SUCCESS_COMMITS" ] && SUCCESS_COMMITS="${SUCCESS_COMMITS},"
              SUCCESS_COMMITS="${SUCCESS_COMMITS}${commit}"
              
            else
<<<<<<< HEAD
              CHERRY_EXIT=$?
              echo "⚠️ Cherry-pick failed with exit code: $CHERRY_EXIT"
              
              # Git durumunu kontrol et
              echo ""
              echo "Git status:"
              git status --short
              
              # Unmerged files var mı? (gerçek conflict)
              if git ls-files -u | grep -q .; then
                echo "🔴 REAL CONFLICT - Unmerged files detected:"
                git ls-files -u
                
=======
              # Hata var - conflict mi yoksa başka bir şey mi kontrol et
              if git ls-files -u | grep -q .; then
                # Gerçekten conflict var (unmerged files)
                echo "⚠️ REAL CONFLICT detected in: $commit"
>>>>>>> ee092da (Improve cherry-pick process in auto-hotfix workflow)
                CONFLICT_DETECTED=true
                [ -n "$CONFLICT_COMMITS" ] && CONFLICT_COMMITS="${CONFLICT_COMMITS},"
                CONFLICT_COMMITS="${CONFLICT_COMMITS}${commit}"
                
<<<<<<< HEAD
                # Conflict'leri resolve et ve commit
                git add -A
                git commit --no-edit -m "WIP: Cherry-pick $commit (⚠️ CONFLICT)"
                
              else
                echo "ℹ️ No unmerged files"
                
                # Staged veya unstaged değişiklik var mı?
                if ! git diff --cached --quiet || ! git diff --quiet; then
                  echo "✅ Changes detected (non-conflict issue) - committing"
                  git add -A
                  git commit --no-edit -m "Cherry-pick $commit (auto-resolved)"
                  [ -n "$SUCCESS_COMMITS" ] && SUCCESS_COMMITS="${SUCCESS_COMMITS},"
                  SUCCESS_COMMITS="${SUCCESS_COMMITS}${commit}"
                else
                  echo "⊘ Empty commit (changes already in test) - skipping"
                  git cherry-pick --abort 2>/dev/null || true
                  git reset --hard 2>/dev/null || true
                  [ -n "$SKIPPED_COMMITS" ] && SKIPPED_COMMITS="${SKIPPED_COMMITS},"
                  SKIPPED_COMMITS="${SKIPPED_COMMITS}${commit}"
=======
                # Conflict'i staged duruma al
                git add -A
                git commit --no-edit -m "WIP: Cherry-pick $commit (⚠️ CONFLICT - needs manual resolution)"
                
              else
                # Conflict yok, başka bir sebep (empty commit vs)
                echo "ℹ️ Cherry-pick failed but no conflicts: $commit (possibly empty)"
                
                # Status'u kontrol et
                git status
                
                # Eğer hiç değişiklik yoksa skip et
                if git diff --cached --quiet && git diff --quiet; then
                  echo "→ Skipping (no changes)"
                  git cherry-pick --skip || git cherry-pick --abort || true
                  [ -n "$SUCCESS_COMMITS" ] && SUCCESS_COMMITS="${SUCCESS_COMMITS},"
                  SUCCESS_COMMITS="${SUCCESS_COMMITS}${commit}"
                else
                  # Değişiklik var ama conflict yok - commit et
                  git add -A
                  git commit --no-edit --allow-empty -m "Cherry-pick $commit (auto-resolved)"
                  [ -n "$SUCCESS_COMMITS" ] && SUCCESS_COMMITS="${SUCCESS_COMMITS},"
                  SUCCESS_COMMITS="${SUCCESS_COMMITS}${commit}"
>>>>>>> ee092da (Improve cherry-pick process in auto-hotfix workflow)
                fi
=======
            echo "🔄 Cherry-picking $commit..."
            
            if git cherry-pick $commit; then
              echo "✅ Success: $commit"
              SUCCESS_COMMITS="$SUCCESS_COMMITS$commit,"
            else
              echo "⚠️ Conflict detected in: $commit"
              CONFLICT_DETECTED=true
              CONFLICT_COMMITS="$CONFLICT_COMMITS$commit,"
              
              # Conflict varsa tüm değişiklikleri staged duruma al
              git add -A
              
              # Staged değişiklik var mı kontrol et
              if ! git diff --cached --quiet; then
                # WIP commit oluştur
                git commit --no-edit -m "WIP: Cherry-pick $commit (⚠️ CONFLICT - needs manual resolution)"
              else
                # Değişiklik yoksa cherry-pick'i skip et
                git cherry-pick --skip
>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)
              fi
            fi
            
            echo ""
          done

<<<<<<< HEAD
          echo ""
<<<<<<< HEAD
          echo "=========================================="
          echo "📊 Final Summary:"
          echo "  ✅ Success: $(echo "$SUCCESS_COMMITS" | tr ',' ' ' | wc -w)"
          echo "  🔴 Conflicts: $(echo "$CONFLICT_COMMITS" | tr ',' ' ' | wc -w)"
          echo "  ⊘ Skipped (already applied): $(echo "$SKIPPED_COMMITS" | tr ',' ' ' | wc -w)"
          echo "=========================================="
=======
          # Output'ları set et
          echo "conflict_detected=$CONFLICT_DETECTED" >> $GITHUB_OUTPUT
          echo "conflict_commits=${CONFLICT_COMMITS%,}" >> $GITHUB_OUTPUT
          echo "success_commits=${SUCCESS_COMMITS%,}" >> $GITHUB_OUTPUT
          
          # Branch'i push et
          git push origin $BRANCH_NAME
>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)

          # Eğer hiç gerçek conflict yoksa ve sadece skip varsa
          if [ "$CONFLICT_DETECTED" = "false" ] && [ -z "$SUCCESS_COMMITS" ] && [ -n "$SKIPPED_COMMITS" ]; then
            echo ""
            echo "✅ All commits already exist in test branch (no sync needed)"
            echo "conflict_detected=false" >> $GITHUB_OUTPUT
            echo "all_skipped=true" >> $GITHUB_OUTPUT
          else
            echo "conflict_detected=$CONFLICT_DETECTED" >> $GITHUB_OUTPUT
            echo "all_skipped=false" >> $GITHUB_OUTPUT
          fi
          
          echo "conflict_commits=$CONFLICT_COMMITS" >> $GITHUB_OUTPUT
          echo "success_commits=$SUCCESS_COMMITS" >> $GITHUB_OUTPUT
          echo "skipped_commits=$SKIPPED_COMMITS" >> $GITHUB_OUTPUT
          
          # Branch'i push et
          git push origin "$BRANCH_NAME"
>>>>>>> bb07eeb (Improve commit synchronization and conflict handling)
          
          echo "🔥 Commits to sync:"
          echo "$MISSING" | while read hash; do
            [ -z "$hash" ] && continue
            MSG=$(git log -1 --format="%s" "$hash")
            echo "  ${hash:0:7} - $MSG"
          done
          
          echo ""
          echo "needs_sync=true" >> $GITHUB_OUTPUT
          echo "commits=$COMMITS_TO_SYNC" >> $GITHUB_OUTPUT
          echo "commit_count=$MISSING_COUNT" >> $GITHUB_OUTPUT
          
      - name: Cherry-pick commits to new branch
>>>>>>> 976f848 (Refactor commit synchronization logic in auto-hotfix.yml)
        if: steps.analyze.outputs.needs_sync == 'true'
        run: |
          git checkout origin/rel/env-test
          git checkout -b auto-hotfix-sync-${{ github.run_number }}
          
          IFS=',' read -ra COMMITS <<< "${{ steps.analyze.outputs.commits }}"
          
          for commit in "${COMMITS[@]}"; do
            if ! git cherry-pick $commit; then
              echo "conflict_detected=true" >> $GITHUB_ENV
              # Conflict durumunda da commit et
=======
          git checkout -b auto-hotfix-sync-${{ github.run_number }} origin/rel/env-test
          CONFLICT_DETECTED=false

          IFS=',' read -ra COMMITS <<< "${{ steps.analyze.outputs.commits }}"

          for commit in "${COMMITS[@]}"; do
            if ! git cherry-pick $commit; then
              # Eğer conflict varsa
              CONFLICT_DETECTED=true
>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
              git add -A
              # Değişiklik varsa commit oluştur
              if ! git diff --quiet; then
                git commit -m "WIP: Cherry-pick $commit (needs conflict resolution)"
              fi
            fi
          done
<<<<<<< HEAD
          
          git push origin auto-hotfix-sync-${{ github.run_number }}
      
=======
          echo "==================================="
          echo "Cherry-pick Summary:"
          echo "  Success: $(echo "$SUCCESS_COMMITS" | tr ',' '\n' | grep -c .)"
          echo "  Conflicts: $(echo "$CONFLICT_COMMITS" | tr ',' '\n' | grep -c .)"
          echo "==================================="

          echo "conflict_detected=$CONFLICT_DETECTED" >> $GITHUB_OUTPUT
          echo "conflict_commits=$CONFLICT_COMMITS" >> $GITHUB_OUTPUT
          echo "success_commits=$SUCCESS_COMMITS" >> $GITHUB_OUTPUT
          
          git push origin "$BRANCH_NAME"
          
>>>>>>> ee092da (Improve cherry-pick process in auto-hotfix workflow)
      - name: Create Pull Request
        if: steps.analyze.outputs.needs_sync == 'true'
        id: create_pr
        uses: peter-evans/create-pull-request@v6
        with:
<<<<<<< HEAD
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: auto-hotfix-sync-${{ github.run_number }}
          base: rel/env-test
          title: |
            ${{ env.conflict_detected == 'true' && '⚠️ [AUTO] Hotfix Sync - Review Needed' || '✅ [AUTO] Hotfix Sync from Production' }}
          body: |
            ## 🤖 Otomatik Hotfix Sync
            
            **Durum:** ${{ env.conflict_detected == 'true' && '⚠️ Conflict tespit edildi - Manuel review gerekiyor' || '✅ Conflict yok - Merge edilebilir' }}
=======

          echo "conflict_detected=$CONFLICT_DETECTED" >> $GITHUB_OUTPUT
          git push origin auto-hotfix-sync-${{ github.run_number }}

      - name: Create Pull Request
        if: steps.analyze.outputs.needs_sync == 'true'
        id: create_pr
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
          CONFLICT_STATUS: ${{ env.conflict_detected }}
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PAT_TOKEN }}
          script: |
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
            const conflictDetected = '${{ steps.cherry_pick.outputs.conflict_detected }}' === 'true';
=======
            const conflictDetected = '$CONFLICT_STATUS' === 'true';
>>>>>>> df6fcc9 (Refactor conflict detection environment variable)
=======
            const conflictDetected = env.conflict_detected == 'true';
>>>>>>> 5c3e7c2 (Fix conflict detection condition in workflow)
=======
            const conflictDetected = env.conflict_detected;
>>>>>>> 6091d88 (Update auto-hotfix.yml)
            const title = conflictDetected 
              ? '⚠️ [AUTO] Hotfix Sync - Review Needed'
              : '✅ [AUTO] Hotfix Sync from Production';
            const statusText = conflictDetected
              ? '⚠️ Conflict tespit edildi'
              : '✅ Conflict yok';

            const commits = "${{ steps.analyze.outputs.commits }}".split(',');
<<<<<<< HEAD
            const commitList = commits.map(c => `- [ ] ${c}`).join('\n');
=======
            const commitList = commits.map(c => `- ${c}`).join('\n');
>>>>>>> ca75c28 (Update commit list formatting in auto-hotfix.yml)
            const prBody = `## 🤖 Otomatik Hotfix Sync
<<<<<<< HEAD

            **Durum:** ${statusText}
>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
=======
                          
            **Durum:** ${statusText}
            
            ### Cherry-pick edilen commit'ler:
            ${{ steps.analyze.outputs.commits }}
            
            ### Checklist:
            - [ ] Değişiklikler incelendi
            - [ ] Conflict çözüldü (varsa)
            - [ ] Test ortamında sorun çıkarmaz
            
            **Kaynak:** rel/env-prod → **Hedef:** rel/env-test`;

>>>>>>> 2e55c77 (Refactor auto-hotfix workflow for clarity)
            
            ### Cherry-pick edilen commit'ler:
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
            ${{ steps.analyze.outputs.commits }}
=======
          github-token: ${{ secrets.PAT_TOKEN }}
          script: |
            const conflictDetected = '${{ steps.cherry_pick.outputs.conflict_detected }}' === 'true';
            const conflictCommits = '${{ steps.cherry_pick.outputs.conflict_commits }}'.split(',').filter(Boolean);
            const successCommits = '${{ steps.cherry_pick.outputs.success_commits }}'.split(',').filter(Boolean);
=======
            ${commitList}
>>>>>>> ca75c28 (Update commit list formatting in auto-hotfix.yml)
=======
            echo "${{ steps.analyze.outputs.commits }}" | tr ',' '\n'
>>>>>>> a943604 (Simplify commit list output in auto-hotfix workflow)
=======
            echo "${{ steps.analyze.outputs.commits }}" | tr ',' '\n' | sed 's/^/- /'
>>>>>>> bebcc58 (Fix formatting for cherry-picked commits display)
            
            const title = conflictDetected 
              ? '⚠️ [AUTO] Hotfix Sync - Conflict Resolution Required'
              : '✅ [AUTO] Hotfix Sync from Production';
            
            const statusEmoji = conflictDetected ? '⚠️' : '✅';
            const statusText = conflictDetected
              ? `${conflictCommits.length} conflict(s) detected`
              : 'No conflicts - Ready to merge';

<<<<<<< HEAD
            let commitList = '';
            
            if (successCommits.length > 0) {
              commitList += '### ✅ Successfully cherry-picked:\n';
              commitList += successCommits.map(c => `- [x] \`${c.substring(0, 7)}\``).join('\n');
              commitList += '\n\n';
            }
            
            if (conflictCommits.length > 0) {
              commitList += '### ⚠️ Conflicts (need manual resolution):\n';
              commitList += conflictCommits.map(c => `- [ ] \`${c.substring(0, 7)}\` ⚠️`).join('\n');
              commitList += '\n\n';
            }

            const prBody = `## 🤖 Automatic Hotfix Synchronization

            **Status:** ${statusEmoji} ${statusText}
            **Total commits:** ${{ steps.analyze.outputs.commit_count }}
            **Workflow run:** #${{ github.run_number }}
            
            ${commitList}
>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)
            
            ### 📋 Review Checklist:
            - [ ] All changes reviewed
            - [ ] Conflicts resolved (if any)
            - [ ] No breaking changes for test environment
            - [ ] CI checks passed
            
<<<<<<< HEAD
            **Kaynak:** rel/env-prod
            **Hedef:** rel/env-test
          labels: |
            hotfix
            auto-sync
            ${{ env.conflict_detected == 'true' && 'needs-review' || 'auto-merge-ready' }}
          assignees: ubeniz
      
      - name: Add comment with instructions
        if: env.conflict_detected == 'true'
        uses: actions/github-script@v7
=======
            ---
            **Source:** \`rel/env-prod\` → **Target:** \`rel/env-test\`
            
            ${conflictDetected ? '> ⚠️ **Action required:** Manual conflict resolution needed before merge' : '> ✅ **Ready to merge:** No conflicts detected'}`;

=======
>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
            const { data: pr } = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: title,
              head: 'auto-hotfix-sync-${{ github.run_number }}',
              base: 'rel/env-test',
              body: prBody
            });

<<<<<<< HEAD
            const labels = ['hotfix', 'auto-sync'];
            if (conflictDetected) {
              labels.push('needs-resolution');
            }

=======
>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr.number,
              labels: labels
            });

            await github.rest.issues.addAssignees({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr.number,
              assignees: ['ubeniz']
            });

            core.setOutput('pull-request-number', pr.number);
<<<<<<< HEAD
            core.setOutput('pull-request-url', pr.html_url);

      - name: Add conflict resolution instructions
=======

      - name: Add comment with instructions
<<<<<<< HEAD
>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
        if: steps.cherry_pick.outputs.conflict_detected == 'true'
=======
        if: steps.analyze.outputs.needs_sync == 'true' && env.conflict_detected == 'true'
>>>>>>> 9ff80ce (Fix condition for adding comment on conflict detection)
        uses: actions/github-script@v7
        env:
          RUN_NUMBER: ${{ github.run_number }}
          PR_NUMBER: ${{ steps.create_pr.outputs.pull-request-number }}
<<<<<<< HEAD
          CONFLICT_COMMITS: ${{ steps.cherry_pick.outputs.conflict_commits }}
>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)
=======
>>>>>>> 2e55c77 (Refactor auto-hotfix workflow for clarity)
        with:
          script: |
<<<<<<< HEAD
<<<<<<< HEAD
=======
            const conflictCommits = process.env.CONFLICT_COMMITS.split(',').filter(Boolean);
            const commitList = conflictCommits.map(c => `- \`${c.substring(0, 7)}\``).join('\n');
            
            const commentBody = `## ⚠️ Conflict Resolution Required

            The following commits need manual conflict resolution:
            ${commitList}

            ### Resolution Steps:
=======
            const commentBody = `## ⚠️ Conflict Çözümü Gerekiyor
<<<<<<< HEAD
<<<<<<< HEAD
>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)

            \`\`\`bash
            # 1. Checkout the branch
            git fetch origin
            git checkout auto-hotfix-sync-${process.env.RUN_NUMBER}

            # 2. Check the conflicted commits (marked as WIP)
            git log --oneline -10

            # 3. If needed, reset to clean state and re-apply
            git reset --hard origin/rel/env-test
            
            # 4. Manually cherry-pick with conflict resolution
            ${conflictCommits.map(c => `git cherry-pick ${c} # resolve conflicts, then: git add . && git cherry-pick --continue`).join('\n            ')}

            # 5. Push the resolved changes
            git push origin auto-hotfix-sync-${process.env.RUN_NUMBER} --force-with-lease
            \`\`\`
<<<<<<< HEAD

            ### Alternative: Use GitHub Web Interface
            You can also resolve conflicts directly in GitHub's web interface.

            ---
            Once conflicts are resolved, this PR will be ready to merge. ✅`;

>>>>>>> b6ad3e5 (Improve auto hotfix workflow with new features)
=======
            
            Conflict çözüldükten sonra bu PR merge edilebilir.`;

>>>>>>> f996fa7 (Refactor auto-hotfix workflow for conflict handling)
=======
            
            \`\`\`bash
            git checkout auto-hotfix-sync-${process.env.RUN_NUMBER}
            # Conflict'leri düzelt
            git add .
            git commit -m "Resolve conflicts"
            git push
            \`\`\`
      
            Conflict çözüldükten sonra bu PR merge edilebilir.`;
      
>>>>>>> 07f3766 (Fix comment formatting and cleanup in auto-hotfix.yml)
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ steps.create_pr.outputs.pull-request-number }},
              body: `## ⚠️ Conflict Çözümü Gerekiyor
              
              \`\`\`bash
              git checkout auto-hotfix-sync-${{ github.run_number }}
              # Conflict'leri düzelt
              git add .
              git commit
              git push
              \`\`\`
              
              Conflict çözüldükten sonra bu PR merge edilebilir.`
            });
<<<<<<< HEAD

      - name: Summary
        if: always()
        run: |
          echo "### 🤖 Hotfix Sync Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.analyze.outputs.needs_sync }}" == "true" ]; then
            echo "- **Status:** Sync needed ✅" >> $GITHUB_STEP_SUMMARY
            echo "- **Commits to sync:** ${{ steps.analyze.outputs.commit_count }}" >> $GITHUB_STEP_SUMMARY
            echo "- **PR:** ${{ steps.create_pr.outputs.pull-request-url }}" >> $GITHUB_STEP_SUMMARY
            
            if [ "${{ steps.cherry_pick.outputs.conflict_detected }}" == "true" ]; then
              echo "- **Conflicts:** ⚠️ Yes - manual resolution required" >> $GITHUB_STEP_SUMMARY
            else
              echo "- **Conflicts:** ✅ None - ready to merge" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "- **Status:** Already synchronized ✅" >> $GITHUB_STEP_SUMMARY
          fi
=======
>>>>>>> 07f3766 (Fix comment formatting and cleanup in auto-hotfix.yml)
=======
                    
\`\`\`bash
git checkout auto-hotfix-sync-${process.env.RUN_NUMBER}
# Conflict'leri düzelt
git add .
git commit -m "Resolve conflicts"
git push
\`\`\`
      
Conflict çözüldükten sonra bu PR merge edilebilir.`;
      
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: process.env.PR_NUMBER,
              body: commentBody
            });

>>>>>>> 2e55c77 (Refactor auto-hotfix workflow for clarity)
